import React from 'react';
import { Component, View, Text, StyleSheet } from 'react-native';

export default FavRoutes ({ navigation }) => (
  <View>

  </View>
)

const styles = StyleSheet.create({
  container: {
    flex: 1,
    backgroundColor: '#FFF',
    alignItems: 'center',
    justifyContent: 'center',
  }
})
